
compounds
    ++ => == && || ->
end compounds

keys
    Bool 'case def 'else fn 'if in Integer let 'of Text 'then 'type 
end keys


define program
     [expression] % | [repeat functionDefinition] | [repeat typeDefinition]
end define

define expression
    [number] | [expression] + [expression]
end define

%define atom
 %   [number] % number kann anscheinend nur positive Zahlen
%end define